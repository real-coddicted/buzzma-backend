meta {
  name: Create System Config
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/system-configs
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "key": "MAX_PAYOUT_PAISE",
    "value": "1000000",
    "description": "Maximum single payout amount in paise"
  }
}
