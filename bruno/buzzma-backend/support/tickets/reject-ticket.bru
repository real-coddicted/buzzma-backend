meta {
  name: Reject Ticket
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/tickets/{{ticketId}}/reject
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "note": "Ticket rejected. Issue cannot be reproduced."
  }
}
